#include <stdio.h>
#include <stdlib.h>

void main (int i) {
    int x[10];

    for(i = 0; i < 10; i++){
        int w = rand() % 100;
        (w == x[10]);
        if (x[10] > 20)
            printf("%d\n", x[10]);
    }
}